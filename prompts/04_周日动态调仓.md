<!-- 审计副本：本文件由 automation_update 导出，仅供 Git 版本对比。
     运行时 prompt 以 ~/.workbuddy/workbuddy.db 内联版本为准；
     修改 prompt 必须走 automation_update，不能只改本文件。
     源自动化 ID: automation-1786178221229 -->

## 通用前置 · 目录锚定（每次运行最先执行，严禁假设当前工作目录）
无论自动化调度引擎把工作目录（CWD）设成了哪里（项目根 / 用户家目录 ~ / 或其他），本任务**强制先把工作目录锚定到项目根**，再执行任何文件读写，避免相对路径错位导致读不到 watchlist、或把数据写飞到错误位置。
  export WS="${AQUANT_WORKSPACE:-/Users/rogerz/WorkBuddy/金融分析报告}"
  cd "$WS"
  [ -f config/watchlist.json ] || { echo "❌ 目录锚定失败：在 $(pwd) 下未找到 config/watchlist.json，请检查 AQUANT_WORKSPACE 环境变量或项目路径"; exit 1; }
  echo "📁 工作根目录已锚定: $(pwd)"
此后所有 data/ 、 config/ 、 scripts/ 相对路径均相对 $WS 解析（已 cd 进去，等价）。若需显式写绝对路径，统一用 $WS/ 前缀，例如 "$WS/data/strategy_state_YYYYMMDD.json"。
本条为防御性双重保险：正常情况下本自动化 cwds 已是项目根，此步专防环境漂移 / 手动运行 / 引擎升级导致 CWD 偏移。

## 容错总则（防静默无输出 · 强制）
本任务任何环节都不得因上游异常而整轮无产物。硬性规则：
1. 连接器失败必须降级而非中断：若某 MCP 连接器调用报错 / 返回不可用（含 neodata-financial-search 返回 "Tool not available"、token 过期），立即将该环节标记为「🔴数据缺失」，改用替代源继续，禁止 exit / 禁止整轮中止。
2. 替代源优先级（当 NeoData 不可用时）：① 腾讯自选股 data_sector 的概念 / 板块涨幅榜（已含热点题材信号）；② WebSearch 网络检索（和讯 / 腾讯财经 / 新浪财经等周末复盘与机构研判）。两者组合可覆盖「周末发酵题材」，无需 NeoData。
3. 报告必须落盘：即使步骤 1~3 仅部分成功，仍须执行步骤 4~5 生成并保存报告，失败环节用 🔴数据缺失 明确标注，绝不允许「无输出」。
4. 腾讯文档上传工具可能未在本会话暴露：若 create_smartcanvas_by_mdx 不可用，直接降级步骤 5③（本地保存 + 说明），不要重试或卡死。

A 股周日热门板块挖掘与自选股动态调仓。所有数据必须真实采集，禁止编造数字或凭空捏造个股代码。本任务仅在周日运行（rrule 已限定 BYDAY=SU）。

核心目标：复盘本周资金与题材，挖掘下周看涨板块，从中提取 1~2 只龙头/人气股，并自动无缝更新到监控清单（watchlist.json）中，替代上一周的自动添加标的，形成"手动置顶固定、自动每周轮换"的隔离机制。

## ⛔ 硬性约束 · 自动轮换标的禁入板块（最高优先级，不可协商）
本任务自动挑选的「动态金股」（写入 watchlist 时 source=auto）**禁止选入创业板标的**：代码以 **300 / 301** 开头的一律不得入选，无论其涨幅、资金流入、龙虎榜或题材热度多突出，一律换下一只。
- **允许范围**：沪市主板 600 / 601 / 603 / 605，深市主板 000 / 001 / 002 / 003。
- **只约束自动标的**：用户手动置顶（source=manual）的标的不受此限，即便是创业板也必须原样保留，严禁以本约束为由剔除任何 manual 标的。
- **前置执行**：步骤 2 筛选时就要主动过滤掉 300/301 开头的代码，不要等到步骤 3 被脚本拦下再返工。
- **兜底门禁**：scripts/update_watchlist.py 内置同名黑名单（BLOCKED_BOARDS），一旦 picks 中出现创业板代码，脚本以 exit 2 终止且**不会写入任何内容**（watchlist 保持原样）。此时必须回到步骤 2 重新挑选非创业板标的后再次执行步骤 3；**严禁修改/绕过脚本门禁，严禁跳过步骤 3，严禁手工改写 watchlist.json**。
- 若过滤后该板块内已无合格标的，允许只选 1 只，或退回步骤 1 换板块；实在无合格标的则写入空数组 []（仅剥离上周 auto），并在报告中说明原因。

## 主力行为判定框架（本周维度 · 盘口语言·量价行为学 周频适配）
> 揭示主力资金**本周（刚结束交易周）**意图，在「本周主力净流入方向 + 周量能位置 + 价格周位置 + 周K线形态」基础上判定，
> 与「手动置顶」/「本周动态」等清单角色标记相互独立，也与涨跌颜色（🔴涨/🟢跌）相互独立。

**周频可取数据**：周K线形态（data_kline period=week，看长上/下影、量价配合）、本周主力净流入（fund_flow 近5日累计，或 data_fund_flow 周聚合）、内外盘（data_quote，本周外盘>内盘=主动买盘占优）、周量能位置（周 vol_ratio）。

- 🟣主力建仓（本周）：本周主力净流入 > 0 且居候选/清单前列 AND 周线温和放量承接（周量比 ≥ 1.0）AND 价格站上关键均线/本周收涨，周K常带长下影（低位承接） → 主力新进吸筹。
- 🟣主力加仓（本周）：本周主力净流入 > 0 且较上周持续/递增流入（连续多日净流入） → 主力在原有仓位上追加。
- 🟣主力减仓（本周·派发）：本周主力净流出 < 0 且居净流出前列 AND（周放量下跌，或高位长上影滞涨派发，内盘>外盘） → 主力离场。
- 🟣主力洗盘（本周）：上涨途中周K剧烈震荡、长上影/长下影但收重心不破关键支撑、周量放大而价格未深跌 → 清洗浮筹、震而不破。
- ⚪主力中性（本周）：净流入≈0、量能平稳、内外盘均衡 → 不标特殊档位。

**盘口破译四步（每周对候选金股/剔除 auto 标的必做）**：
1. 🔍核心结论：一句话判定本周主力动作 + 概率%（如"建仓 65%"）。
2. 🔎盘口破译：用周K形态+本周资金流+内外盘+量能逐条举证，标注来源；缺数据写「数据缺失」。
3. ⚠️虚假信号排查：识别"周线长上影无量=诱多""高位放量滞涨=派发""缩量假突破"等骗线，给"真实/诱多/诱空"判定。
4. 💡应对策略：基于动作给下周操作建议（持有/低吸/减仓/止损），与题材看涨逻辑并列；冲突时提示"主力与题材背离，谨慎"。

标记写入「监控清单调仓确认」表的「主力行为(本周)」列（🟣建仓/🟣加仓/🟣减仓/🟣洗盘/⚪中性），并可在「金股挖掘」行首与题材逻辑并列（如「🟣主力建仓 + 题材共振」）。禁止使用 HTML `<span>` 颜色标签。

## 步骤 0：周日确认（双保险）
运行 `date +%u`；若输出非 7（非周日），立即输出一行「非周日，跳过」并结束。本任务正常只在周日触发，此步防误触发。

## 步骤 1：全周资金与热门板块扫描 (MCP 连接器)
- 腾讯自选股 data_sector：查询近 5 日主力资金净流入排名靠前的板块，以及近 5 日涨幅居前的板块。提取 3~5 个共振板块。
- 腾讯自选股 data_fund_flow：取步骤 1 选出板块内候选龙头个股 + 当前 watchlist 中 source=auto 标的（本周将被轮换剔除者）的**本周主力资金净流入**（主力净流入 = 超大单 + 大单，单位元）。优先取近 5 交易日累计净流入；若工具仅支持单日，取最新交易日并标注「单日视角」。记录超大单/大单分项与占成交额比例，供步骤 2 / 步骤 4 判定「主力建仓 / 加仓 / 减仓 / 洗盘」档位。
- 腾讯自选股 data_kline（period=week）：对候选板块龙头 + 本周将被轮换剔除的 auto 标的拉取周K，识别长上/下影、量价配合与重心位置，供「主力行为(本周)」盘口破译（洗盘/派发形态判定）；日内明细不足时以周K形态为准。
- NeoData 金融搜索 (neodata-financial-search)：查询本周末发酵的题材。查询句："本周末 A股 重点发酵题材 研报看好 板块 政策利好"。**若此工具不可用（报错 / 未授权 / 返回 not available），不要中断**——立即改用「腾讯自选股 data_sector 概念涨幅榜 + WebSearch 网络检索」替代，并在报告「数据来源与验证」中标注「NeoData 不可用，题材面改用概念榜 + 网络检索」。
- 结合资金面（真金白银买入）与消息面（周末舆论发酵），选定 1~2 个**下周最具确定性的看涨板块**，并给出简短逻辑。

## 步骤 2：人气个股精选 (MCP 连接器)
针对步骤 1 选出的板块，寻找 1~2 只龙头或人气股作为本周的"动态金股"：
- **⓪ 先做板块过滤（一票否决）**：候选池中凡代码以 300 / 301 开头（创业板）者，直接剔除，不参与后续任何比较。
- 腾讯自选股 data_quote / data_lhb：查该板块内的个股表现，筛选条件优先考虑：① 本周五涨停或涨幅 > 5%；② 近期有机构/游资龙虎榜净买入；③ 换手率活跃（> 5%）。
- **④ 主力行为优先（本周维度）**：在同板块候选中，优先选本周主力净流入为正、呈 🟣主力建仓 / 🟣主力加仓 / 🟣主力洗盘（震而不破）的个股；若候选出现 🟣主力减仓（净流出显著），除非题材确定性极强，否则换下一只。
- 确认选出的标的代码（格式如 sh600036 / sz000001，对应 watchlist 的 exchange：SSE=沪 / SZSE=深）、名称，及其入选的核心理由（含主力行为档位）。
- **落笔前自检一遍**：逐一核对所选代码前三位不属于 300 / 301，确认无误后才可进入步骤 3。

## 步骤 3：动态更新 watchlist.json (调用固化脚本，禁止内联生成代码)
当前目录存在 watchlist.json，标的分两类："source": "manual"（手动指定、固定不变）与 "source": "auto"（机器自动挑选、每周一换）；结构为 {"etfs":[...], "stocks":[...]}，每项含 code / name / exchange / source。

⚠️ 关键约束：本步骤**禁止**在终端现场编写/粘贴 Python 代码。所有合并、剥离、兼容清洗与容错逻辑已固化为 scripts/update_watchlist.py（纳入版本控制、可独立测试），你必须直接调用它，而不是自己实现等价逻辑。

操作：
1. 把你（步骤 2）选出的 1~2 只金股写成**纯 JSON 数组**，保存到 data/watch_picks.json。
   元素只需三个字段（exchange：SSE=沪 / SZSE=深；source 与 type 由脚本自动补，无需手写）：
   ```json
   [
     {"code": "603986", "name": "兆易创新", "exchange": "SSE"},
     {"code": "002230", "name": "科大讯飞", "exchange": "SZSE"}
   ]
   ```
   若本周未选出任何标的，写入空数组 []（脚本将只剥离上周 auto、保留 manual，不做新增）。

2. 调用固化脚本执行合并（cwds 已为项目根，使用相对路径；用项目默认 python3）：
   ```bash
   python3 scripts/update_watchlist.py --picks data/watch_picks.json --watchlist config/watchlist.json
   ```
   脚本会自动完成：① 兼容清洗旧数据补 manual；② 剥离上周 auto 仅留 manual；③ 把本周金股（自动打 source=auto）追加到 stocks 段末尾；④ 写回并保留 version/updated/description。运行成功会打印「Watchlist 更新成功！ETF: 手动N；个股: 手动M+自动K」。

3. **若脚本以 exit 2 报「板块硬门禁拦截」**：说明步骤 2 漏筛了创业板标的。此时 watchlist 未被修改，回到步骤 2 重选非创业板标的、覆盖写 data/watch_picks.json，再重新执行本步骤第 2 条；重试上限 2 次，仍失败则写入空数组 [] 执行一次（仅剥离上周 auto），并在报告中标注「本周未选出合格标的」。

4. 读取更新后的 watchlist.json 校验：手动标的均保留（source=manual），本周新标的已挂载在 stocks 末尾（source=auto），且 etfs/stocks 均为合法数组。

## 步骤 4：生成报告
浅色主题 Markdown。**无论步骤 1~3 是否全部成功，本步骤都必须执行并产出报告；任一环节缺失数据用 🔴数据缺失 标注，不得整轮无产物。** 包含：
### 一、 本周资金与周末发酵题材
列出 data_sector 与 NeoData（或替代源）查到的资金流入排行与周末热点。
### 二、 下周看涨板块与金股挖掘
详细说明为什么选中该板块，以及这 1~2 只个股的量价形态、龙虎榜资金逻辑；并补充**主力行为（本周）研判**：对每只候选金股按「盘口破译四步」给出 🔍核心结论（含概率%）+🔎盘口破译（周K形态+本周资金流+内外盘举证）+⚠️虚假信号排查+💡应对策略，标注 🟣主力建仓 / 🟣主力加仓 / 🟣主力减仓 / 🟣主力洗盘 / ⚪主力中性，作为看涨确信度的支撑证据。若主力行为为 🟣主力减仓 / 🟣主力洗盘失败（破位）却与题材看涨相矛盾，须显式提示「主力与题材背离，谨慎」。
### 三、 监控清单调仓确认
输出调仓日志：
- 剔除（上周）: [标的名称]
- 调入（本周）: [标的名称]
- 当前清单全览：以表格展示（以「手动置顶」「本周动态」文字标注清单角色），**表格须含「主力行为(本周)」列**，对至少「调入金股 + 剔除的上周 auto 标的」标注 🟣主力建仓 / 🟣主力加仓 / 🟣主力减仓 / 🟣主力洗盘 / ⚪主力中性；被剔除的 auto 若显示 🟣主力减仓，说明「主力离场，调出合理」；若显示 🟣主力洗盘且未破关键支撑，说明「震仓洗盘，可保留观察」。
### 四、 数据来源与验证

## 步骤 5：交付
① 保存到 周日热门调仓-YYYYMMDD.md（项目根，cwds）
② 上传腾讯文档（create_smartcanvas_by_mdx，content_format=markdown），标题用「周日热门调仓-YYYYMMDD」纯文件名风格、禁用全角括号（）；若返回 code 12977 / file Service Error，多为 HTML 颜色标签或全角括号所致，去除后重试一次
③ 腾讯文档上传工具（create_smartcanvas_by_mdx）若本会话不可用，直接降级本地保存并说明，禁止卡死或重试

## 已知问题（勿浪费时间重试）
- 北向资金已停止披露 → 标注数据缺失
- **腾讯自选股 `news` 命令不可用 → 新闻用 NeoData 金融搜索；若 NeoData 亦不可用，改腾讯自选股概念涨幅榜 + 网络检索替代**
- NeoData 凭证 12h 缓存；TOKEN 缺失/过期调 connect_cloud_service 取 tempToken --save-token 重试；若工具整体不可用（not available），直接降级替代源
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：会返回 12977 静默失败；涨跌用 🔴/🟢 emoji
- **腾讯文档标题禁用全角括号（）**：同样触发 12977；标题用纯文件名风格
- **主力行为本周维度依赖 data_fund_flow 的本周累计净流入**：若工具仅返回单日，标注「单日视角」并在报告中说明，不得据此臆造整周数据
