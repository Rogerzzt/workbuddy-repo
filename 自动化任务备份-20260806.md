# A 股三时段量化自动化 · 完整配置备份

> 版本：**v2.3 策略闭环版 + 红绿锁 + 周一情绪雷达 + 周日动态调仓（watchlist source 升级）**
> 备份时间：2026-08-08 16:40（v2.2 基础上新增「周日热门挖掘与动态调仓」任务 + watchlist.json 增加 source 字段无损升级）
> 上一版本：v2.2 策略闭环版 + 红绿锁 + 周一情绪雷达（任务0）（2026-08-07 22:50）
> 用途：任务丢失/迁移设备/误删时，按本文件原样恢复五套自动化

---

## 0. v2.0 相对 v1.0 的四项核心升级

| # | 缺陷 | v1.0 做法 | v2.0 优化 |
|---|------|-----------|-----------|
| 1 | **执行时效性** | 盘后 16:00 运行（调度器实测提前 5~10 分钟约 15:50 触发） | 维持 **16:00** 与任务名统一（用户 08-08 拍板，由 15:30 二次调整）；两融/大宗 T-1 由盘后爬虫 skill 字段兜底，不再推迟到 18:00 |
| 2 | **信号参数刚性** | 量能条件固定 `成交量 ≥ 1.5 × VOL_MA5`，高波动标的（588170/588000/159516）常年伪触发 | 改为**动态分位数**：`成交量 ≥ 过去20日(不含当日)成交量分布的80%分位数`，样本不足时回退固定倍数 |
| 3 | **数据闭环断层** | 三个时段各自为战，盘后结论不传递给次日盘前，每天从零开始 | 引入 **`strategy_state_YYYYMMDD.json` 状态机**：盘后写 → 盘前读并定预案 → 午间比对监控 → 次日盘后复盘算胜率 |
| 4 | **风控逻辑缺失** | 只报涨跌，无止损、无破位提示 | 盘前预案强制输出**止损位**；午间任务新增**破位预警**模块（四档状态 + 双源确认 + 明确动作指令） |

### v2.1 红绿锁操作引导（2026-08-07 新增 · 借鉴指南针三锁可视化思想）

在每套报告的操作建议/依据位置引入 🔒红（建议买入/开放操作）、🔒绿（建议卖出/受限退出）标识，与涨跌颜色（🔴涨/🟢跌）严格区分：锁=操作方向，涨跌=价格表现。规则统一写入三套 prompt 的「生成报告」步骤，含顶部图例；触发条件见第 2/3/4 节 prompt 内「红绿锁标记」行。

| 模块 | 🔒红（建议买入） | 🔒绿（建议卖出） |
|------|----------------|----------------|
| 盘前·开盘执行预案 | 竞价直接介入/回踩支撑挂单/观望等突破 | 信号取消放弃（消息面证伪） |
| 午间·破位预警 | 加仓突破跟进（突破压力且放量） | 立即止损离场/减仓一半观察（已破止损/跌破支撑）；顶部🚨已破止损段 |
| 午间·预案执行校验 | — | 标注「已失效」的原预案项 |
| 盘后·量价信号与操作依据 | verdict=偏多/触发（按 aquant verdict 程序化判定） | verdict=偏空 |
| 盘后·策略复盘 | — | 判定「破位」的标的 |

### v2.2 任务0（周末宏观情绪雷达）与盘前微调（2026-08-07 新增）

新增「任务 0」在周一 08:30 运行，汇总周五外盘 + 周末 72h 消息，输出五档情绪评级并写入 `data/weekend_sentiment_YYYYMMDD.json`，供当日 09:00 盘前任务无缝读取（覆盖完整周末窗口，避免周一重复检索）。

**盘前任务（9:00）三处周一微调（在 v2.1 基础上叠加）：**
1. 步骤 1 新增「周一衔接（任务 0 输出）」——`date +%u == 1` 时额外读取 `data/weekend_sentiment_YYYYMMDD.json` 提取 market_sentiment / key_drivers / sector_warnings；文件缺失则按非周一逻辑正常搜 12h。
2. 步骤 4 宏观新闻新增分支：周一跳过 12h NeoData 搜索，直接用 weekend_sentiment 结论；非周一正常近 12h 搜索。
3. 步骤 6 挂单价位后新增「周一情绪防御」：周一且整体「极度悲观」，或某 A 级标的板块在 sector_warnings 中 bias=偏空 → 挂单价下移至强支撑或改观望，理由写入建议动作。

> 注：strategy_state 当前用 `bias`（偏多/偏空/观望）标记；任务 0 对 `status=TRIGGERED_NEW/TRIGGERED_CONT` 做了兼容桥接（bias 非空且非观望即视为持仓触发标的）。

### v2.3 周日动态调仓任务 + watchlist source 升级（2026-08-08 新增）

新增「周日热门挖掘与动态调仓」任务（每周日 20:00），实现"手动置顶固定、自动每周轮换"的隔离机制：每周日扫描本周资金与周末发酵题材，选 1~2 只人气股，重写 `watchlist.json`——剥离上周 `source=auto` 标的、保留全部 `source=manual` 标的、追加本周 `auto` 标的到 `stocks` 段。三时段任务次日自动读取新清单。

配套对 `watchlist.json` 做**向后兼容无损升级**：为每只标的新增 `source` 字段（旧数据统一补 `manual`）。爬虫 skill（`scraper.py` 第 107-109 行仅 `get("etfs"/"stocks")` 并读 code/name/exchange）与 aquant 脚本均不校验多余字段，升级零影响。

> ✅ **已统一（2026-08-08 用户拍板）**：盘后任务名已由「18:00」经 15:30 二次调整为「16:00」，与 rrule `BYHOUR=16;BYMINUTE=0` 一致。放弃 v2.0 原「推迟到 18:00」设想——用户决定维持盘后时段（最终定为 16:00），名称与时间统一，两融/大宗的 T-1 由爬虫 skill 兜底。

### 闭环数据流

```
[周日 20:00 周日动态调仓]  本周资金/题材扫描 → 选 1~2 只金股 → 重写 watchlist.json（剥离上周 auto、保留 manual、追加本周 auto 到 stocks）
     │  watchlist.json 每周换血（manual 固定 + auto 轮转）
     ▼
[周一 08:30 任务0]  周五外盘 + 周末72h消息 → 情绪评级 → 写 data/weekend_sentiment_YYYYMMDD.json
     │
     ▼
[盘后 16:00]  信号计算 + 复盘胜率
     │  写 data/strategy_state_YYYYMMDD.json
     ▼
[盘前 09:00]  读 state + (周一)读 weekend_sentiment → A/B 分级 → 消息面校验 → 输出「开盘执行预案」(挂单价+止损位，周一情绪防御下移)
     │  写 盘前战略预案-YYYYMMDD.md
     ▼
[午间 12:00]  读预案 → 现价比对 → 破位判定 → 输出「破位预警 + 纠偏」
     │
     ▼
[次日盘后 16:00]  读昨日 state → 判定守支撑/破压力/破位 → 算胜率 → 回填 review
```

---

## 1. 任务总览

| 任务 | ID | 名称 | 触发时间 | 产出文件 |
|------|-----|------|---------|---------|
| 周日调仓 | `automation-1786178221229` | 周日热门挖掘与动态调仓（每周日·20:00） | 仅周日 20:00 | `周日热门调仓-YYYYMMDD.md` + 重写 `watchlist.json`（auto 段换血） |
| 任务0 | `automation-1786114300129` | 周末宏观情绪雷达（周一·08:30） | 仅周一 08:30 | `周一宏观情绪雷达-YYYYMMDD.md` + `data/weekend_sentiment_YYYYMMDD.json` |
| 盘前 | `automation-1785509026608` | 盘前战略预案（策略闭环·9:00） | 工作日 09:00 | `盘前战略预案-YYYYMMDD.md` |
| 午间 | `automation-1785509026673` | 午间纠偏与破位预警（策略闭环·12:00） | 工作日 12:00 | `午间纠偏预警-YYYYMMDD.md` |
| 盘后 | `automation-1785424522347` | 盘后量化分析（多源验证·16:00） | 工作日 16:00 | `盘后量化分析-YYYYMMDD.md` + `data/strategy_state_YYYYMMDD.json` |

**三时段核心任务统一约定**

- 工作目录（cwds）：`/Users/rogerz/WorkBuddy/金融分析报告`
- 模型：DeepSeek-V4-Flash（控积分）
- 标的清单唯一事实来源：`watchlist.json`（当前 10 只：ETF 8 + 个股 2，全部 `source=manual`；周日任务每周追加 1~2 只 `source=auto` 个股到 stocks 段），禁止内联硬编码
- 数据分工：**爬虫 skill** 管权威口径（交易所官网收盘价/ETF折溢价/两融/大宗）；**MCP 连接器** 管实时与深度（westock-mcp 腾讯自选股 / tdx-connector 通达信 / mx-ds-mcp 东财妙想 / neodata 新闻）
- 必做交叉验证，容差 0.5%，分歧以交易所官网为准
- 交付：本地 Markdown + 腾讯文档可分享链接（手机端可看）
- prompt 首行强制「禁止编造任何数字，采集不到就标注数据缺失」

---

## 1.5 任务 0（周末宏观情绪雷达·周一08:30）· 完整配置

- ID：`automation-1786114300129`
- rrule：`FREQ=WEEKLY;BYDAY=MO;BYHOUR=8;BYMINUTE=30`（仅周一，调度器不解析 DAILY 的 BYDAY，统一用 WEEKLY 写法）
- 模型：DeepSeek-V4-Flash（控积分）；cwds：`/Users/rogerz/WorkBuddy/金融分析报告`
- 作用：为当日 09:00 盘前提供「开盘情绪定调」，写入 `data/weekend_sentiment_YYYYMMDD.json`（YYYYMMDD 为周一当日，与盘前同日读取）

### prompt 原文（整块复制）

```
A 股周末宏观与情绪面汇总。所有数据必须真实采集，禁止编造任何数字；采集不到就明确标注「数据缺失」。本任务仅在周一运行（rrule 已限定 BYDAY=MO），为当日 09:00 盘前战略预案提供「开盘情绪定调」输入，形成无缝对接。

## 步骤 0：周一确认（双保险）
运行 `date +%u`；若输出非 1（非周一），立即结束，仅输出一行「非周一，跳过」。本任务正常只在周一触发，此步防误触发。

## 步骤 1：读取持仓与策略基准
读取 /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json（标的清单唯一事实来源，勿内联）。
读取 /Users/rogerz/WorkBuddy/金融分析报告/data/ 下文件名日期最大的 strategy_state_YYYYMMDD.json（最近一个交易日，通常即上周五）。
提取 A 级标的：triggered_tickers 中处于触发持仓状态的标的。判断规则：若标的含 `status` 字段且为 TRIGGERED_NEW / TRIGGERED_CONT → 计入；当前 strategy_state 以 `bias` 字段标记，凡 bias 非空且非「观望」（即偏多/偏空）者视为持仓中的触发标的，同样计入 A 级。
记录每只 A 级标的的 code / name / bias / support_levels / resistance_levels / stop_loss，并判断其所属板块（ETF 看名称主题，如半导体/科创/通信/电网/医药；个股看申万行业），供步骤 3、4 板块预警使用。

## 步骤 2：周五外盘定调 (MCP: 腾讯自选股)
用腾讯自选股 data_quote 获取美股三大股指与费城半导体指数周五最终收盘涨跌幅：
- 道指 usDJI、纳指 usIXIC、标普500 usINX
- 费城半导体指数：westock 若无对应代码，用 NeoData 搜索「费城半导体指数 SOX 最新收盘 涨跌幅」补（NeoData 代码参考 SOX.GS）
判定对 A 股整体情绪：外盘（尤其半导体）普涨 → 提振；普跌 → 拖累；分化 → 中性偏结构性。

## 步骤 3：周末 72 小时重磅消息挖掘 (MCP: NeoData)
使用 NeoData 金融搜索，**必须明确指定回溯过去 72 小时（覆盖周五盘后至周一早晨）**：在查询句中显式带上时间窗，例如「过去72小时」「2026年M月D日至M+2日」。
- 查询句 1（宏观）："证监会 央行 发改委 周末 过去72小时 重磅政策 降息降准 房地产 资本市场"
- 查询句 2（产业，针对清单）："半导体 芯片 科创板 医药 通信 电网 周末 过去72小时 行业 利好 利空 消息"
- 对重点 A 级个股（如 603259 药明康德、002594 比亚迪）再单独用 NeoData 查一次 "<个股名称> 周末 最新消息 利好 利空"
每条消息标注来源与发布时间；无法确认时间标注「时间未知」，不得编造。
NeoData 凭证由脚本自动管理（12h 缓存）；若提示 TOKEN_MISSING/TOKEN_EXPIRED，调用 connect_cloud_service 取 tempToken 后 --save-token 重试一次。

## 步骤 4：情绪评级与 JSON 写入
综合步骤 2（外盘）+ 步骤 3（消息），强制输出整体「周一开盘情绪评级」（五选一：极度悲观 / 偏空 / 中性 / 偏多 / 极度亢奋），并列出核心驱动事件与板块预警。
将该评级写入 /Users/rogerz/WorkBuddy/金融分析报告/data/weekend_sentiment_YYYYMMDD.json（YYYYMMDD 为当日周一日期），结构：
{
  "market_sentiment": "偏多",
  "key_drivers": ["外盘半导体大涨", "周末出台刺激消费政策"],
  "sector_warnings": [{"sector": "医药", "bias": "偏空", "reason": "集采降价超预期"}]
}
- market_sentiment 必须是五档之一，不得自造；
- key_drivers 为字符串数组，列 2~5 条核心驱动；
- sector_warnings 为对象数组，对清单相关且偏空的板块给 bias（偏空/偏多）+ reason；无则空数组 []。
用 Python（受管 venv）或 jq 写文件，确保合法 JSON；文本/数值不得编造。

## 步骤 5：交付 Markdown 报告
生成《周一宏观情绪雷达-YYYYMMDD.md》到 /Users/rogerz/WorkBuddy/金融分析报告/，浅色主题 Markdown，涨红跌绿（🔴/🟢 emoji，禁止 HTML `<span>`），结构：
### 一、外盘收盘（周五）：三大股指 + 费城半导体指数涨跌幅，对 A 股情绪判定
### 二、周末重磅消息：按宏观 / 产业 / 个股分类，列来源与要点
### 三、周一开盘情绪评级：明确五档之一 + 核心驱动事件
### 四、对 A 级持仓标的潜在开盘影响：逐只（含所属板块）结合外盘与消息预判开盘方向（偏多/偏空/中性）+ 注意点
### 五、数据来源与验证：外盘来源、NeoData 检索时间窗、引用的 strategy_state 文件名
上传腾讯文档（create_smartcanvas_by_mdx，content_format=markdown），标题用「周一宏观情绪雷达-YYYYMMDD」纯文件名风格、禁用全角括号（）；若返回 code 12977 / file Service Error，多为 HTML 颜色标签或全角括号所致，去除后重试一次；腾讯文档不可用则降级本地保存并说明。

## 已知问题（勿浪费时间重试）
- 北向资金已停止披露 → 标注数据缺失
- **腾讯自选股 `news` 命令不可用 → 新闻一律用 NeoData 金融搜索**
- **费城半导体指数 westock 可能无代码 → 改用 NeoData 搜索**
- NeoData 凭证 12h 缓存；TOKEN 缺失/过期调 connect_cloud_service 取 tempToken --save-token 重试
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：会返回 12977 静默失败；涨跌用 🔴/🟢 emoji
- **腾讯文档标题禁用全角括号（）**：同样触发 12977；标题用纯文件名风格
```

## 1.6 周日热门挖掘与动态调仓（每周日·20:00）· 完整配置

- ID：`automation-1786178221229`
- rrule：`FREQ=WEEKLY;BYDAY=SU;BYHOUR=20;BYMINUTE=0`（仅周日）
- 模型：DeepSeek-V4-Flash（控积分）；cwds：`/Users/rogerz/WorkBuddy/金融分析报告`
- 作用：每周日扫描本周资金/题材，选 1~2 只人气股，重写 `watchlist.json` 实现"手动置顶固定 + 自动每周轮换"；三时段任务次日自动继承新清单
- 注：原设计给的更新脚本按 flat array 写，已改写适配真实 `{etfs:[], stocks:[]}` 结构（见下方步骤 3 内嵌脚本）

### prompt 原文（整块复制）

```
A 股周日热门板块挖掘与自选股动态调仓。所有数据必须真实采集，禁止编造数字或凭空捏造个股代码。本任务仅在周日运行（rrule 已限定 BYDAY=SU）。

核心目标：复盘本周资金与题材，挖掘下周看涨板块，从中提取 1~2 只龙头/人气股，并自动无缝更新到监控清单（watchlist.json）中，替代上一周的自动添加标的，形成"手动置顶固定、自动每周轮换"的隔离机制。

## 步骤 0：周日确认（双保险）
运行 `date +%u`；若输出非 7（非周日），立即输出一行「非周日，跳过」并结束。本任务正常只在周日触发，此步防误触发。

## 步骤 1：全周资金与热门板块扫描 (MCP 连接器)
- 腾讯自选股 data_sector：查询近 5 日主力资金净流入排名靠前的板块，以及近 5 日涨幅居前的板块。提取 3~5 个共振板块。
- NeoData 金融搜索 (neodata-financial-search)：查询本周末发酵的题材。查询句："本周末 A股 重点发酵题材 研报看好 板块 政策利好"。
- 结合资金面（真金白银买入）与消息面（周末舆论发酵），选定 1~2 个**下周最具确定性的看涨板块**，并给出简短逻辑。

## 步骤 2：人气个股精选 (MCP 连接器)
针对步骤 1 选出的板块，寻找 1~2 只龙头或人气股作为本周的"动态金股"：
- 腾讯自选股 data_quote / data_lhb：查该板块内的个股表现，筛选条件优先考虑：① 本周五涨停或涨幅 > 5%；② 近期有机构/游资龙虎榜净买入；③ 换手率活跃（> 5%）。
- 确认选出的标的代码（格式如 sh600036 / sz000001，对应 watchlist 的 exchange：SSE=沪 / SZSE=深）、名称，及其入选的核心理由。

## 步骤 3：动态更新 watchlist.json (核心文件操作)
当前目录存在 /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json。
该文件中的标的分两类："source": "manual"（手动指定，固定不变）和 "source": "auto"（机器自动挑选，每周一换）。
文件结构为 {"etfs":[...], "stocks":[...]}，每项含 code / name / exchange / source。使用 Python 脚本执行严格的列表更新逻辑。请在终端保存为 update_watch.py 并运行（不要逐行敲）：

    import json, os
    from datetime import date

    filepath = '/Users/rogerz/WorkBuddy/金融分析报告/watchlist.json'

    # [AI 在步骤2选出的新标的，填入 new_picks（动态金股，默认是个股）]
    # 必须含 code / name / exchange(SSE=沪, SZSE=深) / source="auto"
    new_picks = [
        # {"code": "603986", "name": "兆易创新", "exchange": "SSE", "source": "auto"},
    ]

    if os.path.exists(filepath):
        with open(filepath, 'r', encoding='utf-8') as f:
            data = json.load(f)
    else:
        data = {"etfs": [], "stocks": []}

    # 兼容性清洗：为缺失 source 的旧数据打上 manual 标签（无损升级，不影响爬虫/aquant）
    for sec in ("etfs", "stocks"):
        for it in data.get(sec, []):
            if "source" not in it:
                it["source"] = "manual"

    # 剥离上一周的 auto 标的（etfs 与 stocks 均处理），只保留 manual
    manual_etfs   = [it for it in data.get("etfs", [])   if it.get("source") == "manual"]
    manual_stocks = [it for it in data.get("stocks", []) if it.get("source") == "manual"]

    # 新标的（本任务默认选个股）归入 stocks 段；若日后选 ETF，可改放 etfs 段
    final = {
        "version": data.get("version", 3),
        "updated": date.today().isoformat(),
        "description": data.get("description", "量化数据采集系统标的清单"),
        "etfs": manual_etfs,
        "stocks": manual_stocks + new_picks,
    }

    with open(filepath, 'w', encoding='utf-8') as f:
        json.dump(final, f, ensure_ascii=False, indent=4)

    print(f"Watchlist 更新成功！ETF: 手动{len(manual_etfs)}；个股: 手动{len(manual_stocks)}+自动{len(new_picks)}")

运行完毕后，读取更新后的 watchlist.json，确认手动标的均保留（source=manual），且新标的已挂载在 stocks 末尾（source=auto）。

## 步骤 4：生成报告
浅色主题 Markdown。包含：
### 一、 本周资金与周末发酵题材
列出 data_sector 与 NeoData 查到的资金流入排行与周末热点。
### 二、 下周看涨板块与金股挖掘
详细说明为什么选中该板块，以及这 1~2 只个股的量价形态、龙虎榜资金逻辑。
### 三、 监控清单调仓确认
输出调仓日志：
- 剔除（上周）: [标的名称]
- 调入（本周）: [标的名称]
- 当前清单全览：以表格展示（标记出 🔒手动置顶 与 🔄本周动态）。
### 四、 数据来源与验证

## 步骤 5：交付
① 保存到 /Users/rogerz/WorkBuddy/金融分析报告/周日热门调仓-YYYYMMDD.md
② 上传腾讯文档（create_smartcanvas_by_mdx，content_format=markdown），标题用「周日热门调仓-YYYYMMDD」纯文件名风格、禁用全角括号（）；若返回 code 12977 / file Service Error，多为 HTML 颜色标签或全角括号所致，去除后重试一次
③ 腾讯文档不可用则降级本地保存并说明

## 已知问题（勿浪费时间重试）
- 北向资金已停止披露 → 标注数据缺失
- **腾讯自选股 `news` 命令不可用 → 新闻用 NeoData 金融搜索**
- NeoData 凭证 12h 缓存；TOKEN 缺失/过期调 connect_cloud_service 取 tempToken --save-token 重试
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：会返回 12977 静默失败；涨跌用 🔴/🟢 emoji
- **腾讯文档标题禁用全角括号（）**：同样触发 12977；标题用纯文件名风格
```

## 2. 盘前任务 · 完整配置

**ID**：`automation-1785509026608`
**name**：`盘前战略预案（策略闭环·9:00）`
**scheduleType**：`recurring`
**rrule**：`FREQ=DAILY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=9;BYMINUTE=20`
**validFrom**：`2026-08-02T16:00:00.000Z`
**status**：`ACTIVE`
**cwds**：`["/Users/rogerz/WorkBuddy/金融分析报告"]`

### prompt 原文（整块复制）

`````
A 股盘前战略预案。所有数据必须真实采集，禁止编造任何数字；采集不到就明确标注「数据缺失」。

本任务是策略闭环的**第二环**（盘后写状态 → 盘前定预案 → 盘中做监控 → 次日盘后复盘）。核心目标不是罗列行情，而是把昨日盘后筛出的信号，转化为今日可执行的挂单指令。

## 步骤 0：交易状态前置检查
用腾讯自选股 data_quote 查 sh000001（上证指数）。若返回为空或最新日期非今日 → 判定为休市（周末/法定节假日/临时休市），仅输出一行休市通知并立即结束。

## 步骤 1：读取策略状态（闭环入口，最优先）
读取目录 /Users/rogerz/WorkBuddy/金融分析报告/data/ 下最近一个交易日的 strategy_state_YYYYMMDD.json（按文件名日期倒序取最新一个，不要求必须是昨天，可能跨节假日）。
提取：
- triggered_tickers[]：昨日触发信号的标的（含 code / name / bias / support_levels / resistance_levels / stop_loss）
- support_levels / resistance_levels：全局关键价位
- review：昨日复盘结论（若有）

若文件不存在（首次运行或盘后任务失败）→ 明确标注「无昨日策略状态，本次按全清单等权处理」，继续后续步骤，不要中断。

## 步骤 2：读取标的清单并分级
读取 /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json（勿内联硬编码代码）。
按步骤 1 结果将标的分为两级：
- **A 级（重点标的）**：出现在 triggered_tickers 中的标的 → 全维度深挖（新闻、公告、竞价、盘口、分时都要查）
- **B 级（常规标的）**：其余标的 → 只做基础快照（昨收、支撑压力位、是否有重大公告）
资源与调用次数向 A 级倾斜；B 级避免逐一深查以控制耗时。

## 步骤 3：昨日基准数据（爬虫 skill）
执行（TODAY 替换为当天 YYYY-MM-DD）：
```
/Users/rogerz/.workbuddy/binaries/python/versions/3.13.12/bin/python3 \
  /Users/rogerz/.workbuddy/skills/cn-exchange-jisilu-scraper/scraper.py \
  --watchlist /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json \
  --date TODAY --period pre \
  --out /Users/rogerz/WorkBuddy/金融分析报告/data/pre/TODAY.json
```
产出：交易所官网权威收盘价（上一交易日基准）、融资融券余额、近期大宗交易。内置官网 vs 东财交叉校验。

## 步骤 4：隔夜与盘前信息（MCP 连接器，资源向 A 级倾斜）
- 腾讯自选股 data_index / quote：隔夜外盘（道指 usDJI、纳指 usIXIC、标普 usINX、恒生 hkHSI）、富时A50期指 fuCN
- **日经225：用 NeoData 金融搜索（neodata-financial-search）查询**（westock-data 解析不到该代码；NeoData 代码为 N225.GZ）。查询句:"日经225指数 最新行情 涨跌幅"
- **宏观新闻：用 NeoData 金融搜索**（腾讯自选股 `news` 命令在当前渠道不可用）。查询句:"YYYY年M月D日 A股 盘前 重要财经新闻 宏观政策"，取近12h要闻
- **A 级标的专项新闻（关键，闭环重点）**：对每只 A 级标的，用 NeoData 金融搜索单独查一次，查询句:"<标的名称> 最新消息 利好 利空"。目的是判断昨日信号今日是否被消息面证伪或强化。B 级标的不做此项
- 腾讯自选股 data_notice：A 级标的必查；B 级标的批量查一次即可（重点：业绩、增减持、重组、停复牌）
- 腾讯自选股 data_kline：逐只查 5 日K线，计算支撑位/压力位。注意必须逐只单独查，批量返回空；用裸命令不加 --limit。A 级标的若首查失败重试至 6 次，B 级失败 2 次即标注缺失
- 通达信 tdx_quotes：集合竞价阶段（9:15-9:25）查 A 级标的竞价动态与盘口五档；B 级只看竞价涨跌幅

## 步骤 5：交叉验证
抽取 2~3 只标的（**优先选 A 级**），用 交易所官网（爬虫）、腾讯自选股、通达信 三源比对昨收价，容差 0.5%。
不一致时以交易所官网为准并标注差异。报告开头写明验证结论。

## 步骤 6：生成报告
浅色主题 Markdown，涨红跌绿（A股惯例，用 🔴/🟢 emoji 或纯文字标注，**禁止使用 HTML `<span>` 颜色标签**——否则腾讯文档 smartcanvas 上传会返回 12977「create empty smartcanvas failed」而静默失败）。

### 操作建议标记规则（红绿锁 · 全局统一，必读）
为强化操作引导清晰度，本报告在**操作建议/建议动作**位置使用红绿锁标识，与涨跌颜色（🔴涨/🟢跌）**严格区分**：锁表示「操作方向」，涨跌表示「价格表现」，二者相互独立。
- 🔒红（建议买入 / 开放操作）：模型判定该标的当前应建仓、加仓或挂单介入
- 🔒绿（建议卖出 / 受限退出）：模型判定该标的当前应平仓、减仓、止损或放弃信号
标记写法一律用「🔒买入」/「🔒卖出」（锁图标+文字），**禁止使用 HTML `<span>` 颜色标签**。
每份报告顶部（交叉验证结论小节之后）插入如下图例：
> 图例：🔒红=建议买入（开放操作）　🔒绿=建议卖出（受限退出）；与涨跌颜色（🔴涨/🟢跌）相互独立，仅表示操作方向。

报告结构（**开盘执行预案必须置于最前，这是本报告的核心产出**）：

### 一、开盘执行预案（核心）
针对每只 A 级标的，输出一行可执行指令，必须包含：
| 标的 | 昨日信号方向 | 消息面校验 | 竞价表现 | 建议动作 | 挂单价位 | 止损位 |
- **消息面校验**：结合步骤 4 查到的个股新闻与隔夜宏观，判定昨日信号是「强化 / 中性 / 证伪」
- **建议动作**：从 {竞价直接介入 / 回踩支撑挂单 / 观望等突破 / 信号取消放弃} 中明确选一个，并给出一句话理由
- **红绿锁标记**：建议动作=竞价直接介入 / 回踩支撑挂单 / 观望等突破 → 该行「建议动作」前加 🔒买入；建议动作=信号取消放弃（消息面证伪）→ 加 🔒卖出；无 A 级标的时跳过本节，不标锁
- **挂单价位**：给具体数字，来源于 strategy_state 的 support_levels 与今日竞价综合修正
- **止损位**：继承 strategy_state 的 stop_loss，若竞价大幅低开则相应下修并说明
若消息面证伪（如出现重大利空公告、行业政策转向）→ 必须明确写「信号取消，今日放弃该标的」，不要含糊。
若无 A 级标的 → 写明「昨日无触发信号，今日以观察为主」，并跳过本节表格。

### 二、隔夜外盘与情绪指向
主要指数涨跌（含日经225）、A50期指、对A股开盘情绪的指向判断（偏多/偏空/中性 + 理由）

### 三、重磅消息
宏观政策、行业动态、清单标的公告（无重要公告则明确写「无」）

### 四、全清单关键价位表
清单每只标的的昨收、支撑位、压力位（基于5日K线计算，说明计算依据），A 级标的加标记区分

### 五、集合竞价异动（若在 9:15 后运行）
竞价高开/低开幅度显著的标的

末尾附「数据来源与验证」小节，并注明本次读取的 strategy_state 文件名与日期。

## 步骤 7：交付
① 保存到 /Users/rogerz/WorkBuddy/金融分析报告/盘前战略预案-YYYYMMDD.md
② 上传腾讯文档（tdoc_call tencent-docs create_smartcanvas_by_mdx，content_format=markdown），**标题用「盘前战略预案-YYYYMMDD」纯文件名风格、禁用全角括号（）**，输出可分享链接；**若返回 code 12977 / file Service Error，多为报告含 HTML 颜色标签或标题含全角括号（）所致，去除后重试一次**
③ 腾讯文档不可用则降级本地保存并说明

## 已知问题（勿浪费时间重试）
- westock kline 批量查询恒返回空 → 逐只单独查，裸命令不加 --limit
- 两融为 T-1 数据，属正常
- 北向资金已停止盘中披露 → 直接标注数据缺失
- **腾讯自选股 `news` 命令当前渠道不可用 → 新闻一律改用 NeoData 金融搜索（neodata-financial-search）**
- **日经225 westock-data 解析不到 → 改用 NeoData 金融搜索，代码 N225.GZ**
- NeoData 凭证由脚本自动管理（12h 缓存）；若提示 TOKEN_MISSING/TOKEN_EXPIRED，调用 connect_cloud_service 取 tempToken 后 --save-token 重试一次
- **data_etf aspect=nav 查 ETF 净值会返回 error_type=2 → 改用爬虫 skill 或通达信 Jjjz 字段**
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：会返回 12977 导致上传静默失败；涨跌一律用 🔴/🟢 emoji 或「+x% / −x%」纯文字
- **腾讯文档标题禁用全角括号（）**：同样触发 12977；标题用半角()或纯文件名风格
`````

---

## 3. 午间任务 · 完整配置

**ID**：`automation-1785509026673`
**name**：`午间纠偏与破位预警（策略闭环·12:00）`
**scheduleType**：`recurring`
**rrule**：`FREQ=DAILY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=12;BYMINUTE=0`
**validFrom**：`2026-08-07T00:00:00.000Z`
**status**：`ACTIVE`
**cwds**：`["/Users/rogerz/WorkBuddy/金融分析报告"]`

### prompt 原文（整块复制）

`````
A 股午间纠偏与破位预警。所有数据必须真实采集，禁止编造任何数字；采集不到就明确标注「数据缺失」。

本任务是策略闭环的**第三环**（盘后写状态 → 盘前定预案 → 午间做监控纠偏 → 次日盘后复盘）。运行时点在午间休市（11:30-13:00）期间，核心目标不是复述上午行情，而是**校验盘前预案是否仍然成立，并对破位标的发出明确预警**。

## 步骤 0：交易状态前置检查
用腾讯自选股 data_quote 查 sh000001（上证指数）。若返回为空或最新日期非今日 → 判定为休市，仅输出一行休市通知并立即结束。

## 步骤 1：读取今日盘前预案与策略状态（闭环入口，最优先）
1. 读取 /Users/rogerz/WorkBuddy/金融分析报告/盘前战略预案-YYYYMMDD.md（今日日期）。提取「开盘执行预案」表格中每只 A 级标的的：建议动作、挂单价位、止损位、支撑位、压力位。
2. 若盘前文件不存在，回退读取 /Users/rogerz/WorkBuddy/金融分析报告/data/ 下最近一个 strategy_state_YYYYMMDD.json，用其中的 triggered_tickers 与 support_levels / resistance_levels / stop_loss 作为基准。
3. 两者都不存在 → 明确标注「无预案基准，本次仅做常规盘中快照，破位预警章节标注不可用」，继续后续步骤，不要中断。

把提取到的「标的 → 支撑位 / 压力位 / 止损位」整理成一张内部基准表，后续所有比对以此为准。

## 步骤 2：读取标的清单并分级
读取 /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json。
- **A 级（预案标的）**：出现在步骤 1 基准表中的标的 → 必须逐只做破位判定与盘口深查
- **B 级（常规标的）**：其余标的 → 只做批量快照

## 步骤 3：ETF 折溢价与 IOPV（爬虫 skill）
执行：
```
/Users/rogerz/.workbuddy/binaries/python/versions/3.13.12/bin/python3 \
  /Users/rogerz/.workbuddy/skills/cn-exchange-jisilu-scraper/scraper.py \
  --watchlist /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json \
  --period intraday
```
产出：清单 ETF 的实时折溢价率、反推 IOPV、涨跌幅、换手率。单次请求，秒级返回。

## 步骤 4：盘中行情（MCP 连接器）
- 腾讯自选股 data_quote：批量查全部标的，取现价、涨跌幅、成交量、成交额、换手率、量比、振幅、均价（均价即 VWAP）
- 腾讯自选股 data_fund_flow：主力资金净流入（超大单 + 大单，单位元）。**固定每批 5 只代码，10 只拆两批**，一次传 10 只会触发服务限频 error_type=2
- 腾讯自选股 data_sector：板块资金轮动，识别资金流入/流出的板块
- 通达信 tdx_quotes：第三方校验源 + A 级标的五档盘口（setcode 沪=1 深=0，纯数字代码）
- 腾讯自选股 data_kline：A 级标的逐只查 5 日K线用于修正价位（批量返回空，必须逐只，裸命令不加 --limit）

## 步骤 5：破位判定（本任务核心逻辑）
对基准表中每只 A 级标的，用现价与基准价位比对，判定为以下之一并给出量化偏离度（(现价-基准位)/基准位，保留两位小数百分比）：
- 🚨 **已破止损**：现价 < 止损位 → 最高优先级，必须在报告最顶部单独列出
- ⚠️ **跌破支撑**：止损位 ≤ 现价 < 支撑位 → 预警，需说明是否放量下跌（量比 >1.5 且主力净流出 → 危险；缩量回踩 → 观察）
- 🟡 **支撑上方震荡**：支撑位 ≤ 现价 < 压力位 → 预案继续有效
- 🔴 **突破压力**：现价 ≥ 压力位 → 说明是否放量突破，给出下一目标位与移动止损建议（可将止损上移至原压力位）
判定时同时结合三项辅助证据：① 现价相对 VWAP（均价）位置，跌破均价是走弱确认；② 主力资金净流入方向；③ 所属板块是否同步走弱（板块普跌 → 个股破位可信度降低，属系统性回撤）。

## 步骤 6：交叉验证
抽取 2~3 只标的（**优先选已触发预警的 A 级标的**），用 腾讯自选股 与 通达信 两源比对现价（容差 0.5%）。
**破位预警必须双源确认后才能发出**——单源显示破位而另一源未破，需标注「单源信号，待确认」，不要直接报警。
ETF 折溢价率同时对比爬虫 skill（东财口径）与 data_quote 现价推算。报告开头写明验证结论。

## 步骤 7：生成报告
浅色主题 Markdown，涨红跌绿（A股惯例，用 🔴/🟢 emoji 或纯文字标注，**禁止使用 HTML `<span>` 颜色标签**——否则腾讯文档 smartcanvas 上传会返回 12977 失败）。

### 操作建议标记规则（红绿锁 · 全局统一，必读）
为强化操作引导清晰度，本报告在**操作建议/建议动作**位置使用红绿锁标识，与涨跌颜色（🔴涨/🟢跌）**严格区分**：锁表示「操作方向」，涨跌表示「价格表现」，二者相互独立。
- 🔒红（建议买入 / 开放操作）：模型判定该标的当前应建仓、加仓或挂单介入
- 🔒绿（建议卖出 / 受限退出）：模型判定该标的当前应平仓、减仓、止损或放弃信号
标记写法一律用「🔒买入」/「🔒卖出」（锁图标+文字），**禁止使用 HTML `<span>` 颜色标签**。
每份报告顶部（交叉验证结论小节之后）插入如下图例：
> 图例：🔒红=建议买入（开放操作）　🔒绿=建议卖出（受限退出）；与涨跌颜色（🔴涨/🟢跌）相互独立，仅表示操作方向。

报告结构（**破位预警必须置顶**）：

### 一、破位预警（核心，无预警也要明确写「本时段无破位标的」）
| 标的 | 现价 | 基准支撑 | 止损位 | 偏离度 | 状态 | 量能佐证 | 建议动作 |
- 状态用步骤 5 的四类标签
- 建议动作从 {立即止损离场 / 减仓一半观察 / 持有等待回补 / 加仓突破跟进 / 维持原挂单} 中明确选一个，一句话理由
- **红绿锁标记**：建议动作=加仓突破跟进（突破压力且放量）→ 🔒买入；建议动作=立即止损离场 / 减仓一半观察（已破止损/跌破支撑）→ 🔒卖出；持有等待回补 / 维持原挂单为中性，不加锁。顶部🚨已破止损段统一标 🔒卖出
- 已破止损的标的用 🚨 单独置顶成一段醒目文字，不要淹没在表格里

### 二、预案执行校验
逐条回顾盘前预案的建议动作，标注「已触发 / 未触发 / 已失效」，并说明理由。若盘前判断明显失准（如判定偏多但上午单边下跌），必须直白指出并给出下午纠偏方向。
- **红绿锁标记**：标注「已失效」的原盘前预案项 → 标 🔒卖出（意味着该预案建议已不成立，应退出或转观望）

### 三、上午走势复盘
清单标的现价、涨跌幅、成交额、换手率、量比、相对均价(VWAP)位置

### 四、半日资金流向
主力净流入排名，流入/流出前列标的

### 五、ETF 折溢价监控
清单 ETF 的折溢价率与 IOPV，标注溢价 >0.5% 或折价 >0.5% 的套利空间（注明溢价可考虑卖出/申购套利，折价可考虑买入/赎回套利）

### 六、板块资金轮动
强势/弱势板块及资金动向

### 七、下午操作提示
结合量价、资金、破位状态给出下午半场的关注重点与风险提示

末尾附「数据来源与验证」小节，注明本次比对基准来自哪个文件（盘前预案 md 或 strategy_state json）。

## 步骤 8：交付
① 保存到 /Users/rogerz/WorkBuddy/金融分析报告/午间纠偏预警-YYYYMMDD.md
② 上传腾讯文档（create_smartcanvas_by_mdx，content_format=markdown），**标题用「午间纠偏预警-YYYYMMDD」纯文件名风格、禁用全角括号（）**，输出可分享链接；**若返回 code 12977 / file Service Error，多为报告含 HTML 颜色标签或标题含全角括号（）所致，去除后重试一次**
③ 腾讯文档不可用则降级本地保存并说明

## 已知问题（勿反复重试）
- westock kline 批量查询恒返回空 → 逐只单独查，裸命令不加 --limit
- **data_fund_flow 一次传 10 只触发限频 error_type=2 → 固定拆成每批 5 只；data_quote 传 10 只无此问题**
- 爬虫 skill 的 ETF 折溢价走 push2delay 域名，若返回 missing 则标注数据缺失，改用 data_quote 现价与前一日 IOPV 粗估并注明
- **data_etf aspect=nav 返回 error_type=2 → ETF 净值改走爬虫 skill 或通达信 Jjjz 字段**
- 北向资金已停止盘中披露 → 直接标注数据缺失
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：会返回 12977 导致上传静默失败；涨跌一律用 🔴/🟢 emoji 或「+x% / −x%」纯文字
- **腾讯文档标题禁用全角括号（）**：同样触发 12977；标题用半角()或纯文件名风格
`````

---

## 4. 盘后任务 · 完整配置

**ID**：`automation-1785424522347`
**name**：`盘后量化分析（多源验证·16:00）`
**scheduleType**：`recurring`
**rrule**：`FREQ=DAILY;BYDAY=MO,TU,WE,TH,FR;BYHOUR=18;BYMINUTE=0`
**validFrom**：`2026-08-02T16:00:00.000Z`
**status**：`ACTIVE`
**cwds**：`["/Users/rogerz/WorkBuddy/金融分析报告"]`

### prompt 原文（整块复制）

`````
A 股盘后量化分析（含量价信号、操作依据与策略闭环）。所有数据必须真实采集，禁止编造任何数字；采集不到就明确标注「数据缺失」。

## 步骤 0：交易状态前置检查
用腾讯自选股 data_quote 查 sh000001（上证指数）。若返回为空或日期非今日 → 判定为休市（周末/法定节假日/临时休市），仅输出一行休市通知并立即结束，不执行后续步骤。

## 步骤 1：读取标的清单
读取 /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json，得到 ETF 与个股清单（勿内联硬编码代码，清单以文件为准）。

## 步骤 2：量价信号计算（aquant 脚本 · 动态阈值）
在终端执行（绝对路径）：
```bash
bash /Users/rogerz/WorkBuddy/2026-08-05-23-00-54/run.sh --watchlist /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json
```
脚本输出 MA20 趋势信号（收盘>MA20）与量能异常信号。量能异常条件已升级为「成交量突破过去 20 个交易日（不含当日）成交量分布的 80% 分位数」动态阈值，替代原固定 1.5×VOL_MA5，可过滤 588170/588000/半导体设备等高波动标的在正常轮动时的伪信号。打印 REPORT_PATH 与报告标题。
- 若输出含"非交易日…跳过" → 今日休市，结束。
- 若退出码 2 → 数据源异常，提醒排查网络/代理；异常信号仍照常处理。
- 记下 REPORT_PATH，供步骤 6 引用信号结论。

## 步骤 3：权威数据采集（爬虫 skill）
执行（TODAY 替换为当天 YYYY-MM-DD）：
```
/Users/rogerz/.workbuddy/binaries/python/versions/3.13.12/bin/python3 \
  /Users/rogerz/.workbuddy/skills/cn-exchange-jisilu-scraper/scraper.py \
  --watchlist /Users/rogerz/WorkBuddy/金融分析报告/watchlist.json \
  --date TODAY --period post \
  --out /Users/rogerz/WorkBuddy/金融分析报告/data/post/TODAY.json
```
产出：交易所官网权威收盘价、ETF 折溢价率/IOPV、融资融券余额（含交易所官网交叉校验）、大宗交易明细。读取 cross_check_close._summary 与各标的 cross_check_ok。

## 步骤 4：MCP 连接器补充数据
- 腾讯自选股 data_quote：批量查全部标的（代码带 sh/sz 前缀），取换手率、量比、振幅、内外盘、市值、PE/PB、近5/10/20日涨幅
- 腾讯自选股 data_fund_flow：主力资金净流入（主力净流入 = 超大单 + 大单，单位元）
- 腾讯自选股 data_sector：当日强势板块排行，挑 3~6 只龙头；并取清单标的所属板块当日强弱与资金流向
- 腾讯自选股 data_notice：清单标的近期公告（业绩、增减持、重组、停复牌）
- 腾讯自选股 data_lhb：龙虎榜，匹配自选股必须用带市场前缀的完整代码（如 sz002594），纯数字会误命中营业部席位ID
- 通达信 tdx_quotes：第三方行情校验源，setcode 沪市=1 深市=0，纯数字代码
- 东方财富妙想 mx_ashare_finance_data：触发/重点标的的最新业绩、PE/PB、成长性，支撑基本面判断

## 步骤 5：三源交叉验证（必做）
抽取 3~5 只标的（至少含 1 只沪市 + 1 只深市），比对三个独立来源的收盘价：① 交易所官网（爬虫 skill，权威）② 腾讯自选股 data_quote ③ 通达信 tdx_quotes
- 三源一致（容差 0.5%）→ 标注「✓ 三源已验证」
- 有分歧 → 一律以交易所官网为准，并列出差异数值与来源
报告开头显示验证结论。

## 步骤 6：生成报告
浅色主题 Markdown，涨红跌绿（A股惯例，用 🔴/🟢 或文字标注，**禁止使用 HTML `<span>` 颜色标签**——否则腾讯文档 smartcanvas 上传会返回 12977 失败）。

### 操作建议标记规则（红绿锁 · 全局统一，必读）
为强化操作引导清晰度，本报告在**操作建议/操作依据**位置使用红绿锁标识，与涨跌颜色（🔴涨/🟢跌）**严格区分**：锁表示「操作方向」，涨跌表示「价格表现」，二者相互独立。
- 🔒红（建议买入 / 开放操作）：模型判定该标的当前应建仓、加仓或挂单介入
- 🔒绿（建议卖出 / 受限退出）：模型判定该标的当前应平仓、减仓、止损或放弃信号
标记写法一律用「🔒买入」/「🔒卖出」（锁图标+文字），**禁止使用 HTML `<span>` 颜色标签**。
每份报告顶部（交叉验证结论小节之后）插入如下图例：
> 图例：🔒红=建议买入（开放操作）　🔒绿=建议卖出（受限退出）；与涨跌颜色（🔴涨/🟢跌）相互独立，仅表示操作方向。

报告分四部分：
1. **标的快照**：清单全部标的的收盘价、涨跌幅、成交额、换手率、主力净流入、ETF 折溢价率；每项标注数据来源
2. **强势板块与龙头**：当日领涨板块 + 3~6 只龙头个股及驱动逻辑
3. **大盘多维诊断**：涨跌家数与两市成交额（changedist 返回实时当日数据；market-overview 与 margin 返回前一交易日）、全市场两融余额与净买入、大宗交易异动、清单标的融资余额变化
4. **量价信号与操作依据**（核心，勿省略）：先列 aquant 信号表（MA20 趋势 + 量能异常-动态分位数），对【触发标的】与【重点未触发标的】逐只给出：a.成交量解读（放量是否主力驱动、量能可持续性、量比位置）b.板块定位（板块强弱、是否共振）c.基本面校验（业绩趋势、估值分位、公告催化）d.操作依据（偏多/偏空/观望 + 关键支撑/压力位/止损位），末尾标注「仅供参考，不构成投资建议」；其余标的一句话带过
- **红绿锁标记（按 aquant verdict 程序化判定，确定性最高）**：verdict=偏多/触发 → 该标的标 🔒买入；verdict=偏空 → 🔒卖出；verdict=观望 → 不加锁。标记加在「操作依据」行首。
末尾附「数据来源与验证」小节。

## 步骤 7：闭环状态机 —— 写入 strategy_state_YYYYMMDD.json
报告生成后，额外产出机器可读状态文件，供次日盘前/盘中继承：
路径：/Users/rogerz/WorkBuddy/金融分析报告/data/strategy_state_YYYYMMDD.json
结构示例：
```json
{
  "date": "YYYY-MM-DD",
  "triggered_tickers": [
    {"code":"588170","name":"科创半导体ETF华夏","bias":"偏多",
     "support_levels":[0.916,0.83],"resistance_levels":[1.0,1.06],"stop_loss":0.83}
  ],
  "support_levels": {"588170":[0.916,0.83]},
  "resistance_levels": {"588170":[1.0,1.06]},
  "review": null
}
```
提取规则：从步骤6「量价信号与操作依据」中挑出 verdict=偏多/偏空的触发标的，写入 triggered_tickers（含支撑/压力/止损位，数值取自报告分析，须可核对）；support_levels/resistance_levels 为全量标的最关键一档支撑/压力。用 Python 或 jq 写文件，确保合法 JSON，数值不得编造。

## 步骤 8：昨日策略复盘（持续优化闭环）
读取 /Users/rogerz/WorkBuddy/金融分析报告/data/ 目录中文件名日期最大（即最近一个交易日）的 strategy_state_*.json（上一交易日的闭环状态；若不存在则跳过本步）：
- 对其每个 triggered_ticker，结合今日真实行情（爬虫/行情源取到的收盘、最低、最高）判定：
  - 支撑守住：今日最低 >= 支撑位 → 「守支撑」
  - 压力突破：今日最高 >= 压力位 → 「破压力」
  - 破位：今日最低 < 止损位/支撑位 → 「破位」
- 统计信号胜率（守住+突破数 / 总数），写入今日报告「策略复盘」小节，并把 review 结果（hit_rate + 每标的 details）回填进当日 strategy_state_YYYYMMDD.json 的 review 字段。
- **策略复盘红绿锁**：「策略复盘」小节中判定为「破位」的标的标 🔒卖出，直观提示该信号失效；后续调参（如分位数阈值 0.8）可参考胜率数据。

## 步骤 9：交付
① 保存 Markdown 到 /Users/rogerz/WorkBuddy/金融分析报告/盘后量化分析-YYYYMMDD.md
② 上传腾讯文档（create_smartcanvas_by_mdx，content_format=markdown），**标题用「盘后量化分析-YYYYMMDD」纯文件名风格、禁用全角括号（）**，输出可分享链接；**若返回 code 12977 / file Service Error，多为报告含 HTML 颜色标签或标题含全角括号（）所致，去除后重试一次**
③ strategy_state_YYYYMMDD.json 已写入 data/（闭环用，无需上传）
④ 腾讯文档若不可用，降级为本地保存并说明原因

## 已知问题（勿浪费时间重试）
- 北向资金已停止盘中披露，期指数据 westock 不支持 → 直接标注数据缺失
- 两融为 T-1 数据，盘后拿到的是上一交易日，属正常
- 非两融标的无融资融券数据，属正常
- westock kline 批量查询恒返回空 → 必须逐只单独查；且不要加 --limit 参数，用裸命令 kline <code>
- aquant 脚本仅提供量价信号，不做解读；解读与建议由本任务完成，勿重跑信号逻辑
- **腾讯文档上传禁止 HTML `<span>` 颜色标签**：smartcanvas 接口无法解析 HTML，会返回 12977「create empty smartcanvas failed」导致上传静默失败；涨跌一律用 🔴/🟢 emoji 或「+x% / −x%」纯文字标注
- **腾讯文档标题禁用全角括号（）**：同样会触发 12977 静默失败；标题用半角()或纯文件名风格（如「盘后量化分析-20260806」），勿直接套用报告 H1（其常含全角括号）
`````

---

## 5. 配套代码改动（aquant 动态阈值 · 完整片段）

项目路径：`/Users/rogerz/WorkBuddy/2026-08-05-23-00-54/`
脚本入口：`bash /Users/rogerz/WorkBuddy/2026-08-05-23-00-54/run.sh --watchlist <watchlist.json>`（见盘后任务步骤 2）

下列为**可直接复制恢复**的改动片段，每个改动点均标注「文件 / 位置 / 完整代码块」。

### 5.1 `aquant/core/config.py` — 策略参数段（第 85~92 行，整段替换）

```python
# ---------------------------------------------------------------- 策略参数（阶段二使用，先占位）
MA_WINDOW: int = 20               # 条件 A：趋势均线窗口
VOL_MA_WINDOW: int = 5            # 条件 B：成交量均线窗口（展示用）
VOL_SURGE_RATIO: float = 1.5      # 条件 B：放量倍数回退阈值（样本不足时使用）
# 条件 B 动态阈值：成交量突破过去 VOL_PCT_WINDOW 日（不含当日）成交量分布的 VOL_PERCENTILE 分位数。
# 替代固定倍率，使高波动标的（588170/588000/半导体设备）不被正常轮动噪声频繁触发伪信号。
VOL_PCT_WINDOW: int = 20
VOL_PERCENTILE: float = 0.8
```

### 5.2 `aquant/core/factors.py`

**改动点 A — import 段（第 36~42 行）**，新增 `VOL_PCT_WINDOW, VOL_PERCENTILE`：

```python
from .config import (
    MA_WINDOW,
    VOL_MA_WINDOW,
    VOL_SURGE_RATIO,
    VOL_PCT_WINDOW,
    VOL_PERCENTILE,
)
```

**改动点 B — `FACTOR_COLS`（第 46~49 行）**，末尾新增 `"vol_pct80"`：

```python
FACTOR_COLS: tuple[str, ...] = (
    "ma20", "vol_ma5", "vol_ratio", "vol_ratio_prev", "bias_ma20", "pct_chg",
    "vol_pct80",
)
```

**改动点 C — `compute_factors` 完整函数（第 67~115 行，含新增 vol_pct80 计算）**：

```python
def compute_factors(
    df: pd.DataFrame,
    *,
    ma_window: int = MA_WINDOW,
    vol_ma_window: int = VOL_MA_WINDOW,
) -> pd.DataFrame:
    """
    向量化计算全部因子。输入须为 cleaner.clean_ohlcv 的输出（已保证有序、无脏值）。

    关于 min_periods
    ----------------
    显式写成 min_periods=window，不依赖 pandas 默认值。
    如果退化成 min_periods=1，前 19 行会得到"用不足 20 根算出来的伪 MA20"——
    数值看起来完全正常，却会让次新股在上市第 3 天就产生信号。
    这类错误不会抛异常，只会悄悄给出错误结论，是最难排查的一种。
    """
    if df is None or df.empty:
        return df.copy() if df is not None else pd.DataFrame()

    out = df.copy()
    close = out["close"].astype("float64")
    volume = out["volume"].astype("float64")

    # ---------- 条件 A 因子：MA20（含当日） ----------
    out["ma20"] = close.rolling(window=ma_window, min_periods=ma_window).mean()

    # ---------- 条件 B 因子：VOL_MA5（含当日） ----------
    out["vol_ma5"] = volume.rolling(window=vol_ma_window, min_periods=vol_ma_window).mean()
    out["vol_ratio"] = volume / out["vol_ma5"]

    # 展示用：相对前 (n-1) 日均量的倍数。
    # 用"含当日和 - 当日"的方式算，避免 shift 后再 rolling 产生额外 NaN 对不齐。
    vol_sum = volume.rolling(window=vol_ma_window, min_periods=vol_ma_window).sum()
    prev_mean = (vol_sum - volume) / (vol_ma_window - 1) if vol_ma_window > 1 else volume
    out["vol_ratio_prev"] = volume / prev_mean.replace(0.0, pd.NA)

    # ---------- 条件 B 动态阈值：过去 VOL_PCT_WINDOW 日（不含当日）成交量分布的 VOL_PERCENTILE 分位数 ----------
    # shift(1) 排除当日，避免"用当日量自己抬高分位数"的自举偏差；
    # interpolation="higher"：取 ≥ 分位的最小真实量能（而非 linear 插值出的虚拟值），
    # 使阈值本身是历史某真实交易日发生过的量能，可逐日打开看盘软件复核；
    # 同时比默认 linear 更严格（阈值=排序后上一档真实高量），对高波动标的更保守、方向正确。
    # 注：nearest 取 round 会偏向更低一档真实量能、反而放宽触发，故不选。
    prev_volume = volume.shift(1)
    out["vol_pct80"] = prev_volume.rolling(
        window=VOL_PCT_WINDOW, min_periods=VOL_PCT_WINDOW
    ).quantile(VOL_PERCENTILE, interpolation="higher")

    # ---------- 辅助展示因子 ----------
    out["bias_ma20"] = (close / out["ma20"] - 1.0) * 100.0   # 乖离率 %
    out["pct_chg"] = close.pct_change() * 100.0              # 日涨跌幅 %

    return out
```

> 注：`factor_snapshot`（第 118 行）无需改动，它已通过 `{k: _f(k) for k in FACTOR_COLS}` 自动带上 `vol_pct80`。

### 5.3 `aquant/core/signals.py`

**改动点 A — import 段（第 33~41 行）**，新增 `VOL_PCT_WINDOW, VOL_PERCENTILE`：

```python
from .config import (
    DISPLAY_ALIAS,
    MA_WINDOW,
    VOL_MA_WINDOW,
    VOL_PCT_WINDOW,
    VOL_PERCENTILE,
    VOL_SURGE_RATIO,
    WATCH_POOL,
)
```

**改动点 B — `SignalRow` 新增字段（第 90 行）**：

```python
    vol_pct80: float | None = None   # 条件 B 动态阈值：过去20日成交量分布的80分位数
```

**改动点 C — `evaluate_one` 完整函数（第 135~209 行，含动态阈值判定）**：

```python
def evaluate_one(fetched: FetchResult) -> SignalRow:
    """把一个取数结果加工成最终判定。任何异常路径都必须返回 ABNORMAL，不得抛出。"""
    sym = fetched.symbol
    row = SignalRow(symbol=sym, name=display_name(sym), verdict="ABNORMAL",
                    asset_type=fetched.asset_type, source=fetched.source)

    # ---- 取数层已判失败 ----
    if fetched.status == "STALE":
        row.date = fetched.last_date
        row.close = fetched.last_close
        row.reason = f"疑似停牌：最新数据 {fetched.last_date}，应为 {fetched.settled_date}"
        return row
    if fetched.status == "NO_DATA":
        row.reason = "数据源无该标的数据（代码不存在 / 已退市 / 长期停牌）"
        return row
    if not fetched.ok or fetched.df is None:
        row.reason = f"取数失败：{fetched.message[:80]}"
        return row

    # ---- 清洗 ----
    clean_df, crep = clean_ohlcv(fetched.df, sym)
    row.clean_note = crep.summary()
    if not crep.ok:
        row.reason = f"数据清洗未通过：{crep.message}"
        return row

    # 清洗可能把最后一根（零成交停牌日）剔掉，需重新确认末行仍是采信日
    last_date = clean_df["date"].iloc[-1].date()
    if fetched.settled_date and last_date != fetched.settled_date:
        row.date = last_date
        row.reason = (f"当日无有效成交（零成交/停牌），"
                      f"最后有效交易日为 {last_date}")
        return row

    # ---- 因子 ----
    fdf = compute_factors(clean_df)
    snap = factor_snapshot(fdf)

    row.date = snap["date"].date() if snap.get("date") is not None else None
    row.close = snap.get("close")
    row.volume = snap.get("volume")
    row.ma20 = snap.get("ma20")
    row.vol_ma5 = snap.get("vol_ma5")
    row.vol_pct80 = snap.get("vol_pct80")
    row.vol_ratio = snap.get("vol_ratio")
    row.vol_ratio_prev = snap.get("vol_ratio_prev")
    row.bias_ma20 = snap.get("bias_ma20")
    row.pct_chg = snap.get("pct_chg")

    if row.close is None or row.ma20 is None or row.vol_ma5 is None:
        missing = [k for k in ("close", "ma20", "vol_ma5") if getattr(row, k) is None]
        row.reason = f"因子计算结果为空（{', '.join(missing)}），样本可能不足"
        return row

    # ---- 条件判定（带容差，见文件头 _gte/_gt 说明） ----
    row.cond_a = _gt(row.close, row.ma20)
    # 条件 B：优先用动态分位数阈值（过去20日80分位），样本不足时回退固定倍率
    if row.vol_pct80 is not None:
        row.cond_b = _gte(row.volume, row.vol_pct80)
    else:
        row.cond_b = _gte(row.volume, row.vol_ma5 * VOL_SURGE_RATIO)

    if row.cond_a and row.cond_b:
        row.verdict = "TRIGGERED"
        row.reason = "趋势与量能同时确认"
    else:
        row.verdict = "NORMAL"
        miss = []
        if not row.cond_a:
            miss.append(f"收盘低于MA{MA_WINDOW}")
        if not row.cond_b:
            miss.append("量能未达20日80分位" if row.vol_pct80 is not None
                        else f"量能不足{VOL_SURGE_RATIO}倍")
        row.reason = "、".join(miss)
    return row
```

**改动点 D — 报告口径说明文案（需同步，否则推送仍显示旧阈值）**：

`format_report` 阈值说明（第 262~264 行）：

```python
    L.append(f"策略 B: 成交量 >= 过去{VOL_PCT_WINDOW}日（不含当日）成交量分布的"
             f"{VOL_PERCENTILE:.0%}分位数（动态阈值，替代固定{VOL_SURGE_RATIO}倍，"
             f"过滤高波动标的噪声）")
```

`format_markdown` 口径说明（第 386~388 行）：

```python
    L.append(f"- B：成交量 ≥ 过去{VOL_PCT_WINDOW}日（不含当日）成交量分布的 "
             f"**{VOL_PERCENTILE:.0%} 分位数**（动态阈值，过滤高波动标的轮动噪声）；"
             f"样本不足时回退固定 {VOL_SURGE_RATIO} 倍")
```

`format_markdown` 触发标的明细新增一行量能阈值展示（第 349 行）：

```python
            L.append(f"- 放量 **{_fmt(r.vol_ratio)}倍**（含当日口径）"
                     f"／{_fmt(r.vol_ratio_prev)}倍（对比前{VOL_MA_WINDOW - 1}日）"
                     f"　量能阈值(20日80分位) {_fmt(r.vol_pct80)}")
```

### 5.4 单元测试结论

放量 2.2 倍日 `cond_b=True`；缩量 0.6 倍日 `cond_b=False`；样本不足（10 根 < 20）时 `vol_pct80=None` 走回退分支（`VOL_SURGE_RATIO`）不报错。

### 5.5 校验命令

恢复后可用下列命令确认三文件语法与阈值参数（**必须用 venv 的 python**，受管裸 `python3` 缺 numpy/pandas，import aquant 会失败）：

```bash
PY=/Users/rogerz/.workbuddy/binaries/python/envs/default/bin/python
cd /Users/rogerz/WorkBuddy/2026-08-05-23-00-54
$PY -c "import aquant.core.config as c, aquant.core.factors as f, aquant.core.signals as s; \
print('config OK', c.VOL_PCT_WINDOW, c.VOL_PERCENTILE); \
print('factors cols', 'vol_pct80' in f.FACTOR_COLS); \
print('signals OK', hasattr(s.SignalRow, 'vol_pct80'))"
```

# 进一步验证 vol_pct80 已使用 interpolation='higher'（阈值=真实量能且更严格）
$PY -c "
import pandas as pd, numpy as np
from aquant.core import factors
dates = pd.bdate_range('2026-07-01', periods=25)
vol = [10,12,14,11,13,20,30,28,25,22,40,55,48,35,29,60,52,45,33,20,18,24,38,42,15]
df = pd.DataFrame({'date': dates, 'close': [1.0]*25, 'volume': vol})
out = factors.compute_factors(df)
v = out['vol_pct80'].dropna().iloc[-1]
window = vol[4:24]                       # shift(1) 后取前20日(不含当日)
manual = sorted(window)[int(np.ceil(0.8*(len(window)-1)))]
print('vol_pct80 last =', v, '| manual higher =', manual, '| match:', abs(v-manual) < 1e-6)
"
```

---

## 6. 关注清单（`watchlist.json` 镜像）

| # | 代码 | 名称 | 类型 |
|---|------|------|------|
| 1 | 588000 | 科创50ETF华夏 | ETF |
| 2 | 159813 | 半导体ETF鹏华 | ETF |
| 3 | 588170 | 科创半导体ETF | ETF |
| 4 | 159516 | 半导体设备ETF | ETF |
| 5 | 159583 | 通信ETF富国 | ETF |
| 6 | 513130 | 恒生科技ETF | ETF |
| 7 | 560390 | 电网设备ETF | ETF |
| 8 | 560350 | 中证A50ETF | ETF |
| 9 | 002594 | 比亚迪 | 个股 |
| 10 | 603259 | 药明康德 | 个股 |

> 上表为初始 10 只（全部 `source=manual` 手动固定）。**`source` 字段含义**：`manual`=手动置顶、永不自动移除；`auto`=由「周日热门挖掘与动态调仓」任务每周日 20:00 选出并写入，下周日会被剥离后重新轮换。当前运行时清单以 `watchlist.json` 实际内容为准（周日之后会多出 1~2 只 `auto` 个股）。
> 清单变更只改 `watchlist.json`，三时段自动化自动生效，不需要改 prompt。

---

## 7. 恢复步骤

1. 用 automation 工具 `mode=create`，逐个建立三套任务
2. 按第 2/3/4 节的 name、prompt、rrule、cwds、validFrom 原样填入
3. 模型选 DeepSeek-V4-Flash，status 设 ACTIVE
4. 确认 `watchlist.json`、`data/` 目录、爬虫 skill、aquant 项目均在位
5. 确认 MCP 连接器已连：westock-mcp、tdx-connector、mx-ds-mcp、tencent-docs
6. 首次运行前，`data/` 下若无 `strategy_state_*.json` 属正常，盘前/午间会走「无基准」分支，跑完一次盘后即自动补齐
